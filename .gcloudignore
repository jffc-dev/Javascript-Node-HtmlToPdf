# Controls what `gcloud run deploy --source .` uploads.
# Without this file, gcloud falls back to .gitignore (which excludes
# package-lock.json and would break `npm ci` in the Docker build).

node_modules
.env
.env.example
.git
.gitignore
npm-debug.log
*.log
